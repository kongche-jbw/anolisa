[capability]
name = "agent-skills"
description = "OS operation skills bundle for Agents to invoke"

[implementation]
components = ["os-skills"]

[requires_env]
os = "linux"
arch = ["x86_64", "aarch64"]
